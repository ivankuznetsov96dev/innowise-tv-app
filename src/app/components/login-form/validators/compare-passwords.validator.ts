import { FormGroup, ValidatorFn } from '@angular/forms';

// export const ComparePasswordsValidator = (
//   form: FormGroup,
//   passwordField: string,
//   confirmPasswordField: string,
// ): ValidatorFn => {
//   const password = form.controls[passwordField].value;
//   const passwordConfirm = form.controls[confirmPasswordField].value;
//   if (!password || !password_confirm) {
//     return null;
//   }
// };

// export function ComparePasswordsValidator(): ValidatorFn {
// }
